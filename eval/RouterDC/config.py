# config.py
"""
Configuration file for the RouterDC system.

This file centralizes all settings for the router model, the expert LLMs,
API configurations, and prompts.
"""
import os
import torch

# --- Security Note ---
# Use environment variables for API keys and avoid committing them to version control.
API_KEYS = {
    "openai": os.getenv("OPENAI_API_KEY"),
    # Add other providers as needed
}

# --- Device and Path Configuration ---
DEVICE = "cuda" if torch.cuda.is_available() else "cpu"

# Path to the SentenceTransformer model for generating embeddings
EMBEDDING_MODEL_PATH = "/gemini/code/embedding"

# Path to the PRE-TRAINED MLP router model file.
# This file should be generated by the 'train.py' script from the MLP project.
ROUTER_MODEL_PATH = "./trained_mlp_classifier.pth"

# Input and Output files
INPUT_FILE_PATH = ""
OUTPUT_FILE_PATH = ""


# --- Router Model Architecture ---
# These dimensions MUST match the architecture of the trained MLP model.
ROUTER_INPUT_DIM = 768
ROUTER_HIDDEN_DIM = 128
ROUTER_OUTPUT_DIM = 2  # ('difficult': 0, 'easy': 1)


# --- Expert LLM Configuration ---
# Define the models to be used based on the router's decision.
SIMPLE_EXPERT_MODEL = {
}

COMPLEX_EXPERT_MODEL = {

}

# --- Prompt Engineering ---

# Define the prompt for the expert models to solve the math problem.
# This is different from classification; we are now asking for a solution.
EXPERT_SOLVER_PROMPT_TEMPLATE = """
Solve the following math problem step-by-step.
At the end, provide only the final numerical answer inside #### markers. For example: ####123####.

Problem:
"{query}"

Step-by-step solution:
"""
